# 09: 布局与光栅化

**做什么：** 实现纹理布局与按格元的光栅化，输出与 pi_sdf 等价的 SDF 纹理。

**关联：** REQ-001.2、REQ-001.3、REQ-001.4、REQ-004.3

**触及：** `src/core/raster/layout_inner.rs`、`src/core/raster/raster_inner.rs`、`src/model/raster/sdf_inner.rs`、`tests/raster.rs`

**阻塞于：** TASK-06、TASK-07

**状态：** open

- [x] CASE-041 / 042 / 044 / 045 / 047 / 089 / 090 / 091 通过
- [ ] CASE-043 / 046（与 pi_sdf 逐像素差 ≤ ±1 灰度）——**推迟到 TASK-15 的 PNG 对比**（用户决定：先不做像素对比，实现完成后统一比 PNG）
- [x] 同一输入两次结果完全一致（CASE-047）；逐像素等价待 TASK-15
- [x] 参数非正与索引越界均返回 Err，不 panic（CASE-042 / 045）

**遗留（待 TASK-15 对拍）：** 零弧格元用 FARWAY(20) 而非 pi_sdf 的 GLYPHY_INFINITY（影响无近邻弧格元的灰度）；is_reverse 语义未归一化；SVG 描边的 width 分支未实现（RasterOptions 无 width 字段）；rasterize 假定 layout 由 grid.extents 算出。
