# 05: 弧拟合与描边

**做什么：** 实现贝塞尔到弧的拟合与描边网格生成。

**关联：** REQ-001.1

**触及：** `src/core/outline/fit_inner.rs`（含内联 `#[cfg(test)] mod tests`）。原计划的 `tests/outline_fit.rs` 不可用——`core` 为 `pub(crate)`，集成测试看不到 `core::outline::fit`；按 plan/06-layout.md 的测试分层改为模块内联测试。

**阻塞于：** TASK-03

**状态：** open

- [x] CASE-024 / 025 / 026 通过（内联 5 用例，含 CASE-076 / 077）
- [x] 非正容差返回 InvalidParam，不 panic

**实现取舍：** 顶点偏移距离 = thickness（描边全宽 2×thickness）；法线判直改用 `d.abs() < 1e-4`（修正参考实现对负曲率弧的误判）；偏差判据改为 64 点采样取最大值（留 0.9 安全系数），非解析上界——需在 TASK-15 对拍确认。
