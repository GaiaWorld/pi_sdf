# 06: 距离场

**做什么：** 实现弧列表的有符号距离采样，确保符号正确。

**关联：** REQ-001.1、REQ-001.3、REQ-005.4

**触及：** `src/core/sdf/sdf_inner.rs`（含内联 `#[cfg(test)] mod tests`）。原计划的 `tests/sdf.rs` 不可用——`core` 为 `pub(crate)`；按 plan/06-layout.md 的测试分层改为模块内联测试。

**阻塞于：** TASK-03

**状态：** open

- [x] CASE-027 / 028 通过（内点与外点符号相反，绝对值等于几何距离；内联 5 用例）
- [x] 越界下标返回 InvalidParam

**遗留：** 符号为「弧局部左负右正」，绝对方向依赖轮廓绕向（逆时针时内负）；`sdf_from_indices` 的 `arc_index` 返回 `all` 中的下标；空弧集返回 `FARWAY` 而非 inf。均需 TASK-15 与 pi_sdf 纹理对拍确认。
