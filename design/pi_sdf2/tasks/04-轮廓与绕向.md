# 04: 轮廓与绕向

**做什么：** 实现子轮廓的构造与遍历、绕向判定与反转（原地生效）。

**关联：** REQ-001.1、REQ-005.3

**触及：** `src/model/outline/contour_inner.rs`、`tests/outline.rs`

**阻塞于：** TASK-03

**状态：** open

- [x] CASE-020 / 021 / 022 / 023 通过（tests/outline.rs，8 用例）
- [x] reverse 与 winding 的改写对传入对象可见（修正副本写入缺陷）
- [x] 测试通过冻结接口 Contour / Outline 调用

**遗留：** `contour_winding` 的 `inverse` 语义按 glyphy 参考实现（`inverse=false` → 顺时针，TrueType 约定），与直觉相反；`even_odd` 按契约取「内=真」（参考实现为「外=真」）。两者需在 TASK-15 等价回归对拍。
