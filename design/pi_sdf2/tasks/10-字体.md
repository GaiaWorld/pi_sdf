# 10: 字体

**做什么：** 实现字体解析、字形索引与度量、字形轮廓提取，并统一 native 与 wasm 的同一签名。

**关联：** REQ-001.1、REQ-002.1、REQ-002.2、REQ-002.3、REQ-004.2

**触及：** `src/api/font/font_face_inner.rs`、`src/api/font/metrics_inner.rs`、`tests/font.rs`

**阻塞于：** TASK-04

**状态：** open

- [x] CASE-048 / 049 / 050 / 051 / 052 / 081 通过（049 / 052 因无 pi_sdf 基线，暂以数学性质断言，待 TASK-15 PNG 对比）
- [x] 损坏字体返回 Err（替换 transmute），不 panic
- [x] native 与 wasm 共用同一 from_bytes 签名，无 _of_wasm 平行方法

**依赖变更：** 新增 `allsorts 0.15.1`（flate2_rust + outline）与 non-wasm 的 `ttf-parser 0.25`；`Cargo.lock` 将 `pathfinder_simd` 锁到 **0.5.4**（0.5.6 用了当前 nightly 已移除的 `core::intrinsics::simd` 内建，导致 aarch64-linux-android 在依赖层就编译失败）。

**遗留：** 测试用系统字体 `C:\Windows\Fonts\segoeui.ttf`（无用例跳过）；CFF 分支已实现但无 .otf fixture 覆盖；advance/ascender/descender 取字体设计单位，是否需归一化待 TASK-15 定。
