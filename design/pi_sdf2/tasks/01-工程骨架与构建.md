# 01: 工程骨架与构建

**做什么：** 建立可编译的 crate 骨架：构建文件、模块声明、全局常量与数值工具、平台分配器与构建脚本，使全部冻结壳芯成为可编译单元。

**关联：** REQ-002.1、REQ-002.2、REQ-002.3、REQ-003.1、REQ-003.2、REQ-003.3、NFR-003、NFR-004、CASE-007、CASE-064、CASE-083、CASE-084、CASE-086

**触及：** `Cargo.toml`、`.gitignore`、`src/lib.rs`、各层与各模块的 `mod.rs`、`src/model/base/consts.rs`、`src/model/base/num.rs`、`src/platform/wasm/allocator.rs`、`src/platform/native/system_font_windows.rs`、`src/platform/native/system_font_android.rs`、`build/`、`tests/skeleton.rs`（除测试外均为豁免冻结的工程文件）

**阻塞于：** 无

**状态：** open

- [x] Cargo.toml 配置 cdylib + rlib、stable 兼容、三目标条件依赖
- [x] 写入 src/lib.rs 与各 mod.rs，声明 core / api / platform 三层
- [x] 写入 model/base/consts.rs 与 num.rs（常量与数值工具）
- [x] 写入 platform/wasm/allocator.rs（wasm 全局分配器，自 lib.rs 迁出）
- [x] ~~整理 build/ 构建脚本~~ → **移入 TASK-14**：pi_sdf2 当前无 build/ 与 wasm/ 消费者（src 零引用），构建脚本与 wasm 前端一起在 TASK-14 落地，届时按「去硬编码路径、去宿主特化」处理（pi_sdf 的 build_wasm.js 含 pi_hal / pi_bon_decode / window._$pi 替换与 temp/cfg.txt 输出目录，均为宿主侧约定，不应留在库的构建脚本里）
- [x] Windows / Android / wasm32 三目标 cargo check 通过（x86_64-pc-windows-msvc、aarch64-linux-android、wasm32-unknown-unknown）
- [x] src/core 检索不到平台 cfg 与 unsafe

**备注：** tests/skeleton.rs 6 个用例通过；src/platform/wasm/allocator.rs 保留 1 处隔离的 unsafe（talc::ClaimOnOom::new 为 unsafe fn）。
