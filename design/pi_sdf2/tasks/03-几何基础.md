# 03: 几何基础

**做什么：** 实现点、向量、直线、线段、贝塞尔、弧与包围盒的全部运算，使几何层可独立验证。

**关联：** REQ-001.1、REQ-005.2、REQ-005.4、REQ-003.1

**触及：** `src/model/geom/primitive_inner.rs`、`src/model/geom/curve_inner.rs`、`src/model/geom/aabb_inner.rs`、`tests/geom.rs`

**阻塞于：** TASK-02

**状态：** open

- [x] CASE-006 / 008 / 009 / 010 / 011 / 012 / 013 / 014 / 015 / 016 / 017 / 018 / 019 通过（tests/geom.rs）
- [x] 测试通过冻结接口 Point / Vector / Line / Segment / Bezier / Arc / Aabb 调用
- [x] Aabb::is_empty 两个分量都判定；Arc 无派生缓存字段

**返工记录：** API-008 的 `to_endpoint` / `from_endpoint`（单点 ArcEndpoint）与 CASE-017 / TERM-004「可无损转换为弧」矛盾，已改为 `endpoints() -> Vec<ArcEndpoint>`（起、终）与 `from_endpoints(Vec<ArcEndpoint>) -> Result<Self>`；`tag` 恢复为线段共享键语义。

**遗留风险：** `line_sub` / `SignedVector::neg` 的符号按契约注释实现，与 pi_sdf 原始实现不同，需在 TASK-06 核对 SDF 符号（REQ-005.4）。
