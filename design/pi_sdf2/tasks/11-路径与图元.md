# 11: 路径与图元

**做什么：** 实现路径动词校验、路径到轮廓、六种图元到轮廓。

**关联：** REQ-001.4、REQ-004.3

**触及：** `src/model/path/path_inner.rs`、`tests/path.rs`

**阻塞于：** TASK-04、TASK-09

**状态：** open

- [x] CASE-053 / 054 / 056 / 057 / 058 / 059 / 082 通过；CASE-055（像素等价）推迟到 TASK-15 的 PNG 对比
- [x] 非法动词判别值返回 Err（替换 transmute UB）
- [x] 六图元统一为一个 Shape opaque 类（内部 ShapeKind + 静态构造器），无样板重复（TASK-03 已完成）

**已知设计冲突（待处理）：** `Path::to_outline` / `Path::sdf_texture` / `Shape::to_outline` 定义在 model 层，但实现必须调用 `core`（弧拟合、网格、光栅），这违反 `plan/05-architecture.md` 的依赖方向约束 1「model 不得依赖 core / api / platform」，也与 CASE-083（依赖单向无环）冲突。需要把这三个方法迁到 api 层（改契约 + 冻结壳），或放宽分层约束——**待用户裁决**。

**遗留：** `PATH_CELL_SCALE = 1.0` 为自选常量；`EllipticalArcTo` 以线段弧近似（非 pi_sdf 的二次贝塞尔拟合）；`path_extents` 对齐 pi_sdf 的原始坐标 min/max；`Shape::line` 的 `step` 语义不明已忽略。
