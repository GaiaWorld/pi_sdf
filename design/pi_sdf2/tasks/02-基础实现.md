# 02: 基础实现

**做什么：** 实现统一错误类型的格式化，并完成常量与数值工具。

**关联：** REQ-004.1、REQ-004.2、REQ-004.3

**触及：** `src/model/base/error_inner.rs`、`src/model/base/consts.rs`、`src/model/base/num.rs`、`tests/base.rs`

**阻塞于：** TASK-01

**状态：** open

- [x] 测试调用冻结接口 error 的 Display，不直接测实现细节（tests/base.rs，4 用例全通过）
- [x] consts.rs / num.rs 完成（TASK-01 已落，tests/skeleton.rs 6 用例覆盖）
- [ ] CASE-003 / CASE-004 / CASE-005——触发路径分别依赖 TASK-10（FontFace）、TASK-14（边界字节）、TASK-09（布局参数），本任务内只保证错误类型自身行为；三个 CASE 随对应任务核销
