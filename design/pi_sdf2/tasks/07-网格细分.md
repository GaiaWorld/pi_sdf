# 07: 网格细分

**做什么：** 实现四叉细分与每格元的近邻弧计算，保证终止性。

**关联：** REQ-001.2、REQ-005.1

**触及：** `src/core/grid/grid_inner.rs`、`src/model/grid/grid_inner.rs`、`tests/grid.rs`、`src/api/entry_inner.rs`（`compute_cell_grid` 的一行转发——`core` 为 `pub(crate)`，测试只能经公共入口 `api::compute_cell_grid` 到达）

**阻塞于：** TASK-03

**状态：** open

- [x] CASE-029 / 030 / 031 / 032 / 033 通过（tests/grid.rs 6 用例，含 CASE-079）
- [x] 任意合法输入都在有限时间内返回（无死循环；`MAX_SUBDIVISION_DEPTH=64` 硬上限）
- [ ] 格元划分与 pi_sdf 一致——近邻弧改用包围盒相交（非 pi_sdf 的弧距离缓存），只能验证二分结构与确定性，逐格对照留待 TASK-15
