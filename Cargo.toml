[package]
name = "pi_sdf2"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = "1.0"
talc = "=4.4.0"

[target."cfg(target_arch = \"wasm32\")".dependencies]
wasm-bindgen = "0.2"

[features]
debug = []
